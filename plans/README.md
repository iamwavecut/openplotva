# Implementation Plans

Generated by the `improve` skill on 2026-06-15, audited against commit `9f32c4b`.
Plans 001–008 were then **executed in parallel isolated git worktrees** and reviewed (see
"Execution run" below). Plans 009 (spike) produced `docs/settings-webapp-auth-design.md` +
plan 010. Plan 011 was discovered during execution.

Each plan is self-contained: read it fully before starting, honor its STOP conditions, run every
verification command, and update your row when done.

**INTEGRATED & GREEN 2026-06-15 (local, not pushed):** at the user's explicit request, ALL plans
(001–011) are merged into `main` and the full workspace is green. `tools/rust-fast-gate.sh` exits 0
end-to-end: `cargo fmt --check` + `cargo check --workspace` + `cargo clippy --workspace ... -D warnings`
+ `cargo test --workspace` all pass. This includes plan 011 (the 3 pre-existing failures are fixed —
notably a real Go-parity regression where `UPDATE_SIDE_EFFECT_MAX_AGE` was 300s vs Go's 60s) and plan
010 (settings WebApp initData auth; the reviewer confirmed the WebAppData HMAC key derivation is
correct). Nothing was pushed. Rollback point: `git reset --hard df4dfa5`.

Merge conflicts resolved along the way (all trivial, union/dedup): duplicate `constant_time_eq`
(002+006, then again 010), a stale one-arg cookie test, `use`-import unions in two test modules, and
the `openplotva-web` Cargo.lock dep list. 002+007 auto-merged in `admin_auth_response` (both changes
verified present + passing). Note: the 010 executor's worktree branched from `9f32c4b`, so its
implementation was merged forward into the integrated `main` and re-verified there.

## Execution order & status

| Plan | Title | Pri | Effort | Depends on | Status |
|------|-------|-----|--------|------------|--------|
| 011  | Fix 3 pre-existing failing tests (make `cargo test --workspace` green) | P1 | M | — | DONE (merged `a7acca5`; workspace green) |
| 001  | CI runs the fast quality gate (clippy + tests) | P1 | S | 011 (else CI goes red) | EXECUTED — code correct, uncommitted (disk ENOSPC during gate) |
| 002  | Make the admin session cookie unforgeable (sign it) | P1 | M | — | EXECUTED — APPROVED (wt `…-788-3`) |
| 003  | Bound the Postgres pool (acquire/idle timeouts) | P1 | S | — | EXECUTED — APPROVED (wt `…-788-4`) |
| 004  | Ingress flood guard recovers from poisoned lock | P2 | S | — | EXECUTED — APPROVED (wt `…-788-5`) |
| 005  | Bound the producer run-report error vector | P2 | S | — | EXECUTED — APPROVED (wt `…-788-6`) |
| 006  | Compare the Telegram auth HMAC in constant time | P2 | S | — | EXECUTED — APPROVED (wt `…-788-7`) |
| 007  | Reject stale Telegram Login data on admin auth | P2 | S | 002 | EXECUTED — APPROVED (wt `…-788-8`) |
| 008  | Move sqlx off the alpha pin onto stable 0.9.0 | P2 | M | 001 | EXECUTED — APPROVED; advisor re-verified: pin+lock `0.9.0`, storage 59/59 tests pass under stable sqlx (wt `…-788-9`) |
| 009  | Design spike: settings WebApp initData auth | P1\* | M | — | DONE — produced `docs/settings-webapp-auth-design.md` + plan 010 |
| 010  | Authenticate the settings WebApp with initData | P1 | L | 006, 009 | DONE (merged `6319da9`; reviewer APPROVE, HMAC derivation verified) |

Status values: TODO | IN PROGRESS | EXECUTED (in a worktree, advisor verdict noted; not merged) |
DONE | BLOCKED | REJECTED.

\* 009 is high-impact but was scoped as a design spike; the buildable work is plan 010.

## Execution run (2026-06-15)

8 executors ran in parallel, each in its own isolated worktree branched from `9f32c4b`, followed
by an independent reviewer re-running the done criteria in the same worktree. Advisor verdicts
(after reading the diffs and reconciling the reviewers' findings):

- **All 8 diffs are correct and in-scope** (no out-of-scope file in any worktree; the main tree
  was never touched — only `plans/`/`docs/` here).
- **002** correctly grew beyond the plan's "4 call sites" estimate: the real admin gate is
  `require_admin_request` (41 sites) + `current_admin_user_id` (3) — the executor threaded the
  signing secret through all of them, in-scope, compiling, with a forged-cookie-rejection test.
  Justified, documented deviation. APPROVED.
- **008** is a clean pin bump; stable sqlx 0.9.0 also drops alpha-only deps (rsa/pkcs8/der) and
  pulls a leaner crypto tree — a net positive. fmt/check/clippy pass; full `cargo test --workspace`
  could not finish (disk — see below). APPROVED, test re-verify pending.
- **001** is correct but **uncommitted**: the executor correctly hit the plan's STOP condition
  when `tools/rust-fast-gate.sh` failed — not on the config change, but because `cargo test`
  errored with **"No space left on device"** (see below) and because the workspace test suite is
  pre-existing-RED (plan 011).

Two issues surfaced by the run, neither a defect in the executors' work:

1. **Disk exhaustion.** Eight cold full-workspace cargo builds in eight separate worktrees
   consumed ~21 GiB of `target/` cache on an already near-full disk (now ~99% used, <1 GiB free).
   This is why plans 001 and 008 could not complete `cargo test`. **Resolved 2026-06-15**: the
   target/ caches of the six approved worktrees (002–007) were deleted, freeing ~59 GiB (disk now
   ~94% used). 001 (config-only) was re-verified structurally; 008 was re-verified warm in its
   worktree — `cargo test -p openplotva-storage` is 59/59 green under stable sqlx 0.9.0.
2. **Pre-existing RED tests on `main`** (now tracked as **plan 011**): `cargo test --workspace`
   already failed on `9f32c4b` before any plan — three tests. This is the concrete proof of plan
   001's value (broken tests landed because CI never ran them) and the reason 001 lists 011 as a
   prerequisite.

Note: `main` advanced to `df4dfa5` (external, during the run); the executed worktrees branched
from `9f32c4b`. Re-run each plan's drift check before merging.

## Recommended merge sequencing (user's call — not done here)

1. **011** — make `cargo test --workspace` green first.
2. **001** — then enable the CI gate (it will be green once 011 lands).
3. Independent: **003**, **004**, **005**, **008** (008 after 001 so CI verifies it).
4. Web/app auth cluster, in this order to resolve shared-region conflicts:
   **002** → **006** → **007** → **010**.

Known textual merge conflicts (trivial to resolve, all in the same regions by design):
- **002 ↔ 006**: both add `constant_time_eq` to `openplotva-web/src/lib.rs` — keep one copy.
- **002 ↔ 007**: both edit `admin_auth_response` in `openplotva-app/src/lib.rs`.
- **010** reuses `constant_time_eq` (from 002/006) and edits the settings gates.

## Dependency notes

- **001 ← 011**: enabling `cargo test --workspace` in CI requires the pre-existing failures fixed,
  or CI is red on day one.
- **007 ← 002**: both modify `admin_auth_response`; sequence 002 → 007.
- **008 ← 001**: the sqlx bump should be gated by the CI test suite.
- **010 ← 006, 009**: reuses the constant-time helper; implements the 009 design.

## Findings considered and rejected (do not re-audit)

- **Callback IDOR** — ownership IS enforced (`delete_drawing.rs:1148` compares `query.from.id`).
- **Taskman terminal records "never pruned"** — false; cleanup worker exists (`app/lib.rs:8545`).
- **N+1 in checkin** — hot path already batches; the loop is an error-fallback only.
- **carapax/async-graphql bumps** — already on latest (carapax 0.38.0, tgbot 0.46.0; no stable
  async-graphql 8.x exists).
- **Startup requeue → double payment** — by-design at-least-once (Go parity); no specific
  non-idempotent executor evidenced.
- **Constant-time compare for the settings signature** — pointless (unkeyed/public algorithm; the
  weakness is the scheme → plan 010).
- **`SELECT *` over-fetch** — sqlx maps to typed structs; marginal.
- **`dequeue_errors` unbounded vec** — does not exist in current code (only `enqueue_errors`, plan 005).

## Findings noted but not planned (raise if priorities change)

- **God modules** (payments.rs 13k, app/lib.rs 13k, dialog_messages.rs 10k, storage/lib.rs 10k,
  aifarm.rs 9k) — prefer opportunistic extraction over a big-bang refactor of tested, churning code.
- **HTTP client builder duplication** across integration crates — low-risk S–M consolidation.
- **No CSP / security headers** on WebApp HTML — defense-in-depth, S.
- **taskman O(n) dequeue/lookup** — known; fix in-memory with per-lane indexes (do NOT move to
  Postgres — shipped as taskman v2 by design).
- **`openplotva-memory` depends on `reqwest`** — borderline vs the AGENTS.md domain-crate rule
  (which has an "owns that boundary" exception); debatable.
- **genai 0.6.0-beta.20 → 0.7.0-beta.3** — minor beta bump; check changelog first.
